apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: milvus-pvc
  namespace: milvus
spec:
  storageClassName: retain-pd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi