apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-pvc
  namespace: milvus
spec:
  storageClassName: retain-pd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
